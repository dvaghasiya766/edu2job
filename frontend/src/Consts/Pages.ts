export enum Pages {
  DASHBOARD = "/",
  LOGIN = "/login",
  REGISTER = "/register",
  VERIFICATION = "/verification",
  AUTHSUCCESS = "/auth-success",
  PROFILE = "/profile",
  LOGOUT = "/logout",
}
